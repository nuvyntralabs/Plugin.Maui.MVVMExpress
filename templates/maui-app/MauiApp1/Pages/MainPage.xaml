<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiApp1;assembly=MauiApp1.Core"
    x:Class="MauiApp1.MainPage"
    x:DataType="local:MainPageViewModel"
    Title="Main">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">
            <Label Text="{Binding Message}" />
            <Label Text="{Binding Count, StringFormat='Count: {0}'}" />
            <Button Text="Increment" Command="{Binding IncrementCommand}" />
            <Button Text="Items" Command="{Binding OpenListCommand}" />
            <Button Text="Add item" Command="{Binding OpenFormCommand}" />
            <Button Text="Sign in" Command="{Binding OpenLoginCommand}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
