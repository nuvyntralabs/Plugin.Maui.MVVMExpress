# MVVMExpress for Visual Studio

Thin VSIX (VS 2022 17.0+). On first use it installs `Plugin.Maui.MVVMExpress.Templates` and runs `dotnet new`. The **IDE extensions** workflow uploads the `.vsix` artifact. See [extensions/README.md](../README.md).
